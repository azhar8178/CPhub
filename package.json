{
  "name": "cphub",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel --stream run dev",
    "build": "cd apps/api && npm install",
    "start": "cd apps/api && node_modules/.bin/tsx src/index.ts",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
