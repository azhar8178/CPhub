DATABASE_URL=postgres://user:pass@localhost:5432/cphub
JWT_SECRET=change-me-to-a-long-random-string
PORT=3001

# Optional — email delivery for password resets & campaigns
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Cloud Partner Hub <hello@cloudpartnerhub.com>"
